[TOC]

# 数字图像处理

## 第一章 绪论

### 1.1 数字图像处理定义

一副图像可以定义为一个二维函数$f(x,y)$，其中$x$和$y$是空间（平面）坐标，任意一对空间坐标$(x,y)$处的幅度值$f$称为图像在该坐标点的**强度或灰度。**

- **数字图像**：当$x,y$和灰度值$f$都是有限的离散量时，该图像称为数字图像。

- **像素 **：数字图像由有限数量的元素组成，这些元素称为图画元素、图像元素或**像素** 。

- **数字图像处理 (Digital Image Processing)**：借助于数字计算机来处理数字图像 。

> **本书界定范围**：输入和输出都是图像的处理，以及从图像中提取特征直至包含各个目标的识别 。



### 1.2 图像处理的三个层次

- **低级处理 (Low-level processing)**：

  - **特点**：输入和输出都是**图像** 。

  - 降噪、对比度增强、图像锐化 。

-   **中级处理 (Mid-level processing)**：
  - **特点**：输入是图像，输出是**从图像中提取的特征**（如边缘、轮廓、目标标识） 。
  - 分割（将图像分为不同区域）、分类（识别各个目标） 。
-   **高级处理 (High-level processing)**：
  
  - **特点**：涉及“理解”已识别的目标，执行与人类视觉相关的认知功能 。



### 1.3 数字图像处理步骤

![img](https://pic2.zhimg.com/v2-a40e9adee3ec100a1db6e13cb25ae73f_1440w.jpg)



## 第二章 数字图像基础

> 2.3之前的内容偏向概念介绍，先按下不记，从2.3开始记录，如有重点后续进行补充（大概率没有后续）

### 2.3 图像获取与感知模型

将照射能量转换为数字图像主要由三种传感器配置：

- 使用单个传感器获取图像
- 使用条带传感器获取图像：如磁共振成像（MRI）和正电子发射断层成像（PET）等。
- 使用阵列传感器获取图像: 如单反相机和手机相机。

#### 2.3.1 简单的成像模型

用二维函数 $f(x,y)$ 表示图像，其幅值由两个分量决定 
$$
f(x,y) = i(x,y) \cdot r(x,y)
$$

1. **入射分量** $i(x,y)$：光源照射到物体上的量，范围 $0 < i(x,y) < \infty$。
2. **反射分量** $r(x,y)$：物体反射光的程度，范围 $0 < r(x,y) < 1$（0为全黑吸收，1为全白反射）。

> [!note]
>
> 对于透射图像（如X光片），使用的是透射系数 $t(x,y)$ 代替反射系数 。

在单色图像（Monochromatic Image）处理中，图像的**亮度（或灰度)**被定义为一个二维函数：
$$
\ell = f(x, y)
$$

- $(x, y)$：表示图像的空间坐标。
- $\ell$：表示在该坐标处的亮度值或灰度值。

### 2.4 图像取样和量化

#### 2.4.1 定义与概念

- **取样**：对坐标 $(x,y)$ 的数字化（决定空间分辨率） 
- **量化**：对**幅值** $f$（灰度值）的数字化（决定灰度分辨率） 

#### 2.4.2 数字图像表示

通常表示为 $M \times N$ 的矩阵

![image-20260215213020006](C:\Users\45924\AppData\Roaming\Typora\typora-user-images\image-20260215213020006.png)

> 原点 $(0,0)$ 或 $(1,1)$ 通常定义在图像的**左上角**，$x$轴向下，$y$轴向右（注意与标准笛卡尔坐标系的差异）

![img](https://pic1.zhimg.com/v2-1ff0ca80b99546f44cc55d73ccb25a8e_1440w.jpg)

1. 数字化参数判定 ($M, N, L$)

图像数字化过程需要确定三个关键参数：

- **$M$ 和 $N$**：图像的空间分辨率（行数与列数）

  - **限制条件**：必须取**正整数**

- **$L$**：离散灰度级数

  - 出于存储效率和量化硬件的考虑，通常取 2 的整数次幂
    $$
    L = 2^k
    $$
     （其中 $k$ 为整数，即**位深**）

  - **范围**：假设灰度是等间隔的，其取值范围为整数区间 $[0, L-1]$
  
    

 2. 动态范围 

- **定义**：系统中**最大可度量灰度**与**最小可检测灰度**之比。

- **决定因素**：
  - 上限：取决于**饱和度**
  - 下限 ：取决于 **噪声**
  
- **特性**：虽然噪声也会出现在较亮的灰度区域，但较暗区域主要由具有“最小可检测灰度”的像素组成，因此下限受噪声影响最大。

  

3. 视觉现象

- **饱和度 ：
  - 指一个阈值（最大值）。
  - **现象**：超过该值的灰度级会被“裁剪”掉
  - **视觉表现**：饱和区域表现为恒定的高灰度级（通常是白色一片），细节丢失
- **噪声**：
  - **现象**：可见噪声表现为**颗粒状纹理模式**
  - **视觉表现**：图中最暗部分的背景通常包含最大的噪声，但人眼通常很难观察到暗背景中的噪声

#### 2.4.3 储存量计算

存储一幅图像所需的比特数 $b$：
$$
b = M \times N \times k
$$

>  一幅 8-bit 的 $1024 \times 1024$ 图像，大小为 $1024 \times 1024 \times 8$ bits = 1 MB。
