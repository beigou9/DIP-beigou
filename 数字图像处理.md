[TOC]

# 数字图像处理

## 第一章 绪论

### 1.1 数字图像处理基础

#### 1.1.1 图像的概念与分类

- **定义**：图像是对客观对象的一种相似性的、生动的描述或表示。
- **分类维度**：
  - **按人眼视觉特点**：
    - **可见图像**：包括生成图像（图形/图片）和光图像（透镜/全息产生）。
    - **不可见图像**：不可见光成像（X射线、红外、超声等）及不可见量成像（温度、压力分布图）。
  - **按波段**：单波段（单亮度值）、多波段（如RGB彩色）、超波段（如遥感图像）。
  - **按连续性**：
    - **模拟图像**：空间坐标和明暗程度连续变化。
    - **数字图像**：空间坐标和灰度均不连续，用离散数字表示（像素的集合）。

#### 1.1.4 数字图像处理的基本特点

- **信息量大**：对计算速度和存储容量要求高。

- **占用频带/带宽宽**：传输和处理难度大，对压缩技术有高要求。

- **像素相关性大**：像素间不独立，存在冗余，压缩潜力大。

- **三维理解需知识导引**：二维图像是三维景物的投影，恢复三维信息需要假设或先验知识。

- **评价主观性**：受人的视觉系统和心理因素影响大。

-----

### 1.4 数字图像处理基本步骤

![img](https://pic2.zhimg.com/v2-a40e9adee3ec100a1db6e13cb25ae73f_1440w.jpg)



## 第二章 X射线冠状动脉造影及图像的后处理

### 2.1 X射线造影概述

#### 2.1.1成像基本原理

- **机制**：基于X射线的穿透性、荧光效应和摄影效应。
- **对比度基础**：人体组织密度和厚度的差异（骨骼高密度$\to$白；肺部低密度$\to$黑）。
- **衰减规律**：X射线强度随穿透深度呈指数衰减。

#### 2.1.2 核心公式：衰减与对比度 

- **衰减公式 (朗伯定律)**：

  $$\Delta N = -\mu \cdot N \cdot \Delta z$$

  - $N$: 入射光子数
  - $\mu$: 线性衰减系数（关键参数，决定成像明暗）
  - $\Delta z$: 穿透厚度

- **对比度公式**：

  $$C = 1 - \exp[-(\mu_2 - \mu_1)l_2]$$

  - $\mu_2 - \mu_1$: 病灶与背景的衰减系数差（差值越大，对比度越高）。

    >  散射线会降低对比度，需用滤线栅抑制。

#### 2.1.3 数字减影血管造影 (DSA)

- **定义**：利用计算机处理消除骨骼和软组织背景，只突出血管的成像技术。

- **时间减影法流程**：

  1. 注入造影剂前拍摄**蒙片 (Mask)**。
  2. 注入造影剂后拍摄**造影像 (Image)**。
  3. **对数减影**。

- **DSA减影公式**：

  $$D(x,y) = k [\ln M(x,y) - \ln I(x,y)] = k \cdot \mu_v \cdot T_v(x,y)$$

  > **为什么取对数？**         
  >
  > 只有取对数，乘积项才变为加减项，相减才能线性地消除背景项 ($\mu_b T_b$)，只保留血管信息 ($\mu_v T_v$)。

- $\Delta z$：厚度，N：光子数，$\Delta N$：减少的光子数，$\mu$：线性衰减系数

------

### 2.2 冠状动脉造影 (CAG)

#### 2.2.1 造影系统与角度

- **单面/双面系统**：单面需分时成像，双面可同步成像（利于三维重建）。
- **造影角度表示**：
  - **左右**：RAO（右前斜，>0），LAO（左前斜，<0）
  - **头足**：CRAN（头位，>0），CAUD（足位，<0）

#### 2.2.2 狭窄衡量指标

- **直径狭窄率**：

  $$\%D_{stenosis} = (1 - \frac{d_{stenosis}}{d_{normal}}) \times 100\%$$

- **面积狭窄率**：

  $$\%A_{stenosis} = (1 - \frac{A_{stenosis}}{A_{normal}}) \times 100\%$$

- **换算关系**：若直径狭窄 $x$，面积狭窄约为 $x(2-x)$。

  - *例*：直径狭窄 50% $\approx$ 面积狭窄 75%

- **临床判定**：直径狭窄 **>50%** 为有意义病变

-----

### 2.3 CAG 图像预处理

- **噪声去除**：
  - **椒盐/随机噪声** $\to$ 中值滤波、高斯平滑
  - **背景不均匀/结构噪声** $\to$ 形态学 Top-Hat 变换
- **畸变校正**：
  - **枕形畸变**（影像增强器曲率导致）
  - **S形畸变**（地磁场影响）
  - *方法*：使用**格网模型**标定校正

-----

### 2.4 二维信息提取 (血管分割)

#### 2.4.1 常用方法

- **导数法**：利用梯度算子检测边缘。
- **Hessian矩阵法**：利用Hessian矩阵的特征值检测管状结构（血管在灰度图上表现为脊线结构)
- **Snake模型 (活动轮廓)**：
  - 能量函数 $E = E_{int} + E_{ext}$
  - $E_{int}$ (内能)：保持曲线光滑、连续
  - $E_{ext}$ (外能)：利用图像梯度吸引曲线贴向血管边缘

#### 2.4.2 骨架提取与拓扑

- **特征点**：
  - **分叉点** ：血管真实分支（3邻域点数>3）。
  - **交叉点**：投影重叠造成的伪交点（需剔除）。
  - **端点** ：血管末梢。

------

### 2.5 血管三维重建 

#### 2.5.1 几何模型

- **透视投影**：X射线源是点光源，成像是**锥形束投影**，存在放大效应。

- **三维直径计算公式**（校正放大率）：

  $$d_{3D} = d_{2D} \cdot \frac{\sqrt{x^2+y^2+z^2}}{\sqrt{u^2+v^2+D^2}}$$

  - 物理意义：物体离射线源越近（$z$越小），投影放大倍数越大。

#### 2.5.2 极线约束

- **定义**：空间一点 $P$ 和两个射线源 $S_1, S_2$ 确定的平面（极平面）与图像平面的交线称为**极线**。
- **作用**：解决双目视觉/双角度造影中的**对应点匹配 (Matching)** 问题。
- **规则**：图像A上的一点 $p_1$，其在图像B上的对应点 $p_2$ **必然位于** $p_1$ 对应的极线上。

#### 2.5.3 重建步骤

1. **几何标定**：求取投影矩阵。
2. **对应点匹配**：利用极线约束和拓扑结构（分叉点匹配）。
3. **三维生成**：从匹配的二维点反解三维坐标。
4. **表面重建**：球包络法、三角面片拼接法。